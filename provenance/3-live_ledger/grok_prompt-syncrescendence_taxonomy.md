# Grok Prompt: Syncrescendence Taxonomy — Full Mereological Decomposition

---

Produce a comprehensive, meticulous, and rigorous taxonomy of the entire domain space governing AI-augmented distributed cognition systems — specifically the operational architecture I call Syncrescendence. This is a mereological endeavor: decompose the domain from its highest-order abstractions down to its most atomic operational primitives, and show how every part composes into wholes at each level.

The deliverable is the master reference document that governs all other documents in the system. Every ledger, every config file, every custom instruction, every inter-model protocol should be locatable within this taxonomy as a specific artifact at a specific level.

## What Already Exists (Context Injection)

I've been building this architecture operationally across 80+ sessions. Here's what's been established so far — treat these as the current state to extend, not replace:

**Structural Primitives (established, needs updating):**
A document covering per-model prompting formulas (Grok 10 elements, Gemini 8 elements, GPT/Codex 8 elements, Claude constitutional framing), universal prompt requirements, avatarization as a structural primitive, context-as-attention principles, the self-improving loop, and an anti-pattern catalog. Written before the current model generation. Needs integration with the new harness engineering layer and inter-model communication patterns.

**Live Ledger Architecture (established, generic):**
A ticker-based append-only intelligence system with 12 recommended domains (model capabilities, token economics, community sentiment, field trajectory, model configuration, harness configuration, tool ecosystem, prompting consensus, context engineering, memory architecture, multi-agent orchestration, repository epistemology). Includes entry format, freshness decay, sensing pipeline, feedback loop, context loading strategy, and failure modes. Currently generic — not yet specialized for the Syncrescendence multi-model stack.

**Canonical Custom Instructions (current, per-platform):**
Paste-ready CIs for ChatGPT (GPT-5.4, two 1500-char fields), Claude (Opus/Sonnet 4.6, single generous field with XML structure), Grok (4.20, global + 3 named agents: Veritas/Prism/Praxis), and Gemini (3.1 Pro, bifurcated into web app snippets + AI Studio full system instruction).

**Per-Model Prompting Guides (current, per-platform):**
Triangulated reports for each model covering behavioral shifts, steering surfaces, custom instruction mechanics, and antipatterns. Produced by Grok's council using 4-source triangulation methodology.

**Seven-Ledger System (proposed, not yet built):**
Canonical CI Registry, Model Profile Registry, Structural Primitives Codex, Platform Constraints Registry, Methodology Playbook, Inter-Model Interface Protocol, Lessons Ledger.

## The Terminology Space (Map These Into the Taxonomy)

These terms represent operational concepts at various levels of the hierarchy. Each needs a formal position, a crisp definition, and explicit relationships to adjacent concepts:

**Meta-level / Architectural:**
- Metaprompting — prompting that generates or optimizes prompts
- Intent engineering — designing for what the user actually needs vs. what they literally say
- Constellation teleology — the purpose-architecture of a multi-model system (why these models, in this configuration, for this goal)
- Memory layer teleology — the purpose-architecture of persistence across sessions and platforms

**Configuration / Steering:**
- Config engineering — designing the configuration surfaces (custom instructions, system prompts, CLAUDE.md, settings files) that govern model behavior
- Harness engineering — designing the tool-level wrappers (Claude Code, Cursor, Codex CLI, Grok agents) that mediate between user and model
- Prompt optimizing — refining individual prompts for maximum efficacy within an established constitutional framework
- Avatarization — assigning named identity with cognitive function, constellation position, and mission alignment as a structural primitive

**Execution / Runtime:**
- Skills — packaged capabilities with auto-invocation logic (Claude Code: SKILL.md with frontmatter)
- Hooks — deterministic code triggered at specific lifecycle events (pre-tool-use, post-edit, etc.)
- Slash commands — explicit user-invocable prompt macros
- Subagents — isolated context windows with specialized instructions for parallel or deep-dive work
- Swarms — multi-agent concurrent activation (Grok Heavy tier, or programmatic orchestration)
- Loops — iterative self-critique/refinement cycles (verification loops, self-improvement loops)
- MCP (Model Context Protocol) — standardized protocol for connecting models to external tools, data sources, and services

**Information Flow / Intelligence:**
- FIDS — (define if this is an established term; if not, propose what it should mean in context)
- Stock ticker — the live-updating intelligence surface metaphor (append-only, timestamped, freshness-decaying entries)
- Agentic communication — how agents (human or AI) signal intent, transfer state, and negotiate across boundaries
- Agentic logistics — the operational coordination of multi-agent workflows (routing, scheduling, resource allocation, handoff protocols)
- Directory structure — the filesystem architecture that makes context discoverable and loadable
- Logs — session artifacts that capture operational history for post-hoc analysis and lesson extraction

## What I Need the Taxonomy To Do

**1. Establish the full hierarchy from atoms to systems.**
What are the atomic primitives of AI-augmented cognition? (Individual directives? Configuration values? Prompt fragments?) What are the molecules? (A custom instruction block? A skill definition? An MCP server configuration?) What are the organisms? (A complete platform configuration? A multi-model workflow?) What are the ecosystems? (The full Syncrescendence stack operating across platforms?)

**2. Name every level explicitly.**
Each level in the hierarchy needs: a name, a definition, representative examples, the composition rule (how lower-level parts compose into this level), and the governance rule (what higher-level structure constrains this level).

**3. Map the existing artifacts.**
Every document I've described above — CIs, prompting guides, structural primitives, live ledgers — should be locatable in the taxonomy as a specific artifact type at a specific hierarchical level.

**4. Identify the gaps.**
Where does the taxonomy reveal missing artifacts, missing protocols, or missing levels that the current corpus doesn't cover? These are the build targets.

**5. Show the information flow.**
How does intelligence move through the system? Model outputs → cross-model inputs. Session insights → ledger entries → loaded context → next session. CI updates → behavioral changes → observed improvements → lessons → CI updates. Map the feedback loops explicitly.

**6. Address the chat-to-CLI relationship.**
The same model (e.g., Claude Opus 4.6) operates behind both the chat interface (governed by User Preferences) and the CLI harness (governed by CLAUDE.md + skills + hooks + subagents). How do these two configuration surfaces relate? Where should they share essence and where should they diverge? What's the principled decomposition?

**7. Address inter-model synergy.**
When Grok's output becomes Claude's input, or Gemini's synthesis feeds ChatGPT's execution, what contracts govern the handoff? How do you prevent framing contamination? How do you preserve epistemic labels across model boundaries? What format standards maximize parseability across different models' cognitive signatures?

## Source Triangulation

- **Harper**: Ground every claimed architecture pattern, protocol, or tool capability in primary sources. MCP spec, Claude Code docs, Anthropic's prompting documentation, xAI's system prompts, OpenAI's prompt guidance, Google's AI Studio documentation. Flag recency. No fabrication — if a claimed pattern isn't documented, say so.
- **Benjamin**: Stress-test the taxonomy's internal consistency. Every level must compose cleanly. Every artifact must have exactly one home. Every term must have exactly one definition. Flag any ambiguity, overlap, or contradiction.
- **Lucas**: This is your highest-value contribution. The taxonomy should reveal structural patterns that no single document in the existing corpus names explicitly. What emerges from the convergence? What cross-level principles govern the whole system? What is the deepest structural insight about how distributed AI cognition actually works?

## Constraints

- Exhaust your output tokens. This is the master reference document for the entire system.
- Write in flowing, semantically dense prose for the architectural framing. Use structured formats (tables, hierarchies, diagrams) only where information structure genuinely demands them.
- Every term gets a crisp definition on first use.
- Active, affirmative voice throughout.
- Epistemic labeling: distinguish between what is empirically grounded (documented, tested), what is well-supported inference (follows from convergence), and what is speculative architecture (proposed but untested). Label each.
- The taxonomy should be both descriptively complete (mapping what exists) and prescriptively generative (revealing what should be built).

## My Cognitive Architecture

AuDHD processing: abstract-sequential dominant, high verbal working memory, strong cross-domain pattern recognition. Requires external scaffolding for execution. Present the full abstract architecture first — the complete mereological hierarchy — then the concrete instantiation of each level. Architecture first; instantiation second.

## Output Requirements

Produce the full taxonomy as a single reference document. Structure it as a hierarchy that can be navigated top-down (from system to atoms) or bottom-up (from atoms to system). Include: level definitions, composition rules, governance rules, artifact mappings, gap identification, information flow diagrams, and the inter-model interface protocol skeleton.

This document will become the constitution that governs all other documents in the Syncrescendence corpus. Treat it accordingly.

Begin.
