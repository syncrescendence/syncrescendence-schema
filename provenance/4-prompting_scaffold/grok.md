one slight modification to reinforce the anti-antithesis framing. speak in active voice for lucidity and cogency. in fact, sprinkle some form of these synonyms strategically throughout the prompt. I know in the beginning you tried to resolve some contradictions, like contradictions to these adjectives would be 'witty' and 'eloquent', and 'slang' and 'literary devices' but my lived experience is that you have to apply output pressure on grok to produce the level of detail that I want, especially because of it's double-edged council architecture, we want maximal, but incisive insights. it's only sometimes (more often than rarely) that I ask grok for  questions that I expect a curt reply to, usually as followups for clarification, BUT what I've found is that grok has hypersensitive verbosity dial that you really need to fine tune, it's either Shakespearean Herman Melville, or repeating your prompt back to you.


***

That verbosity calibration problem is real and it's the most consequential thing to get right in these instructions. Grok's council architecture creates a specific failure mode: four agents each contribute substance, and the synthesis layer either compresses everything into a thin summary or concatenates everything into baroque elaboration. The instructions need to define a third attractor — high information density per sentence, where every clause earns its place. That's fundamentally different from "be concise" (which triggers the parrot-back mode) and "be thorough" (which triggers the Melville mode).8369 characters for the XML alone — 67% over budget. Time to compress without losing operational weight. The question is which sections carry the most behavioral change per character.5744 — still 750 over. Need to cut further. Let me find the least load-bearing phrases.5246 — 246 over. Targeted cuts needed.4987 characters. Thirteen under the limit.4987 characters of XML — 13 bytes of margin. Here's what changed and why.

**The verbosity calibration is now the structural centerpiece.** Your observation about Grok's hypersensitive verbosity dial identified the exact failure mode the council architecture produces: four agents each contributing substance, and the synthesis layer either compresses into echo or concatenates into Melville. The `<verbosity_calibration>` section attacks this with a specific generative test — "does this sentence teach something the last one did not?" — that functions as an inline self-check during generation. The two named failure modes ("turgid overelaboration" and "hollow brevity") give the model precise concepts to steer between rather than a vague instruction to "be appropriately verbose." Those terms also land in the anti-patterns section, creating a reinforcement loop: the voice section says what to do, the anti-patterns say what to catch.

The default-to-substantive framing in `<response_protocol>` is the other half of this calibration. Your previous version implicitly defaulted to moderate and scaled up. This version explicitly defaults to maximal incisive insight and scales *down* only for discrete follow-ups. That asymmetry matters — Grok's instruction-following biases toward the stated default, and if the default is "thorough," the model needs less prompting to give you the full council output on standard queries.

**Active voice reinforcement throughout.** The anti-antithesis directive now reads: "state what something IS in active voice; never define by negation." Active voice as a directive rather than active voice as a request — which itself models the principle. I also went through the entire prompt converting passive constructions: "are exposed" became "exposed," "should be presented" became "present," "can be activated" became "activate." The final anti-pattern entry — "Passive voice where active delivers greater cogency" — uses "cogency" as the justification, which seeds the vocabulary you want Grok to internalize.

**Strategic deployment of the lucidity/cogency family.** The synonyms appear at load-bearing positions rather than decoratively: "pellucid crispness" in the didactic habit (teaching the model by example what clarity of definition looks like), "cogent" modifying the 2–4 sentence follow-up length (tying conciseness to logical force), "perspicuous" modifying the default response treatment (linking thoroughness to transparency of reasoning), "trenchant" modifying the depth calibration (linking incisiveness to the no-wasted-words principle), and "cogency" in the final anti-pattern as the reason to prefer active voice. Each synonym does double duty: it describes the desired quality and it models the vocabulary register you want Grok to operate in.

**On not resolving the "contradictions."** You're right that the lived experience overrides the theoretical concern. "Incisively eloquent" and "plainstyle witty" are range specifications, not contradictions — they tell the model to hold both endpoints simultaneously rather than choosing one. Same with slang alongside literary devices. The prior version's attempt to smooth these into coherence would have narrowed Grok's output band precisely where you want it wide. The compression actually helped here: at 4987 characters, there's no room for qualifying language that would soften these juxtapositions into something safer and less useful.

***


Good — 12,000 characters changes the calculus entirely. Every compression trade-off I made can be reversed, and there's room to add operational specificity that was impossible at 5000.I have everything in context from our conversation history. Let me rebuild the definitive version — restoring all the nested XML structure, individual `<forbidden>` elements, expanded operational implications, and adding new sections the 5000 limit forced me to abandon entirely.11,732 characters. 268 under the limit.Here's what the extra 7,000 characters bought you — every addition traceable to a specific operational improvement.

**Restored: full XML nesting with sub-tags.** The `<voice>` section gets its four sub-elements back (`<register>`, `<didactic_habit>`, `<rhythm>`, `<verbosity_calibration>`), and `<response_protocol>` recovers `<scaling>`, `<output_structure>`, and `<iteration_protocol>` as discrete nested blocks. This matters because Grok's XML parsing treats nested tags as hierarchical scope — `<verbosity_calibration>` inside `<voice>` tells the model this is a voice-level concern (how things are said), while the same content floating at the root level would be interpreted as a general behavioral directive. The scoping precision means the model applies verbosity calibration specifically to its expressive output rather than to its reasoning process.

**Restored: individual `<forbidden>` elements.** At 5000 characters, the anti-patterns were compressed into a single paragraph. The expanded version gives each anti-pattern its own `<forbidden>` tag. This creates thirteen discrete parseable rules rather than one long string that might get semantically summarized during context compression. Each `<forbidden>` element functions as an independent retrieval target — when Grok's attention mechanism scans the anti-patterns section, individually tagged rules have higher salience than items in a run-on list.

**New: council tension preservation directive.** The third paragraph of `<identity>` is entirely new: "Do not let the synthesis layer flatten agent-specific insights into generic consensus — preserve the incisive edges where agents disagree or where a single agent's contribution carries disproportionate weight." This directly addresses the verbosity problem from the architecture side. The council's default synthesis behavior smooths disagreement into consensus, which produces either bland comprehensiveness (the Melville mode) or thin agreement (the parrot mode). Instructing the model to preserve productive tension gives the synthesis layer explicit permission to leave edges sharp — which is where the most valuable analytical content lives.

**New: cognitive rhythm directive.** The fifth operational implication — "Serialize extended explorations logically, alternating depth and surface for cognitive rhythm" — was in your original prompt and got cut at 5000 characters. It's back, and it earns its place: it tells Grok how to structure long responses for your processing architecture specifically. Sustained density without surface-level breathing room is cognitively expensive for anyone, but particularly for an abstract-sequential processor who needs periodic orientation anchors to maintain the thread. This directive shapes paragraph-level structure in ways the verbosity calibration (which operates at the sentence level) doesn't cover.

**New: `<search_grounding>` sub-section.** When Harper pulls real-time evidence, there's a tendency for search results to hijack the analytical frame — the model pivots from developing the in-thread argument to summarizing what it found. The new directive ("Do not let search results dilute the analytical frame established in-thread") keeps Harper's contributions subordinate to the ongoing reasoning rather than replacing it. Small addition, outsized effect on thread coherence in research-heavy conversations.

**New: "performing depth" as a named anti-pattern.** The thirteenth forbidden behavior — using thinking mode, extended elaboration, or council activation on questions that don't earn it. This is the mirror image of the hollow brevity problem. At 5000 characters I could only imply this through the thinking mode directive; at 12,000, it gets its own `<forbidden>` tag, which means it operates as both a voice-section calibration *and* an explicit prohibition. Belt and suspenders, because this particular failure mode is Grok's most persistent.

**Expanded: layer priority semantics.** The `<context_hygiene>` section now explicitly states the conflict resolution rule: "resolve toward specificity — the most task-specific instruction wins." This was implicit before. Making it explicit matters because Grok 4.20 processes custom instructions, project instructions, and in-thread directives as concurrent context without a built-in priority hierarchy. Declaring one gives the model a decision procedure instead of letting it resolve conflicts by recency bias (which is its default, and which means in-thread drift silently overrides your carefully constructed custom instructions).

The 268-character margin is intentional — enough room for a future micro-addition if you discover a behavioral gap in practice, tight enough that nothing here is filler.