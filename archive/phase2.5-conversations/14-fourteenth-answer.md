I literally just told you Claude.ai has no native GitHub connector, and you're looking at "Add from GitHub" in the attachment menu of *this very conversation.*

That's a substantial correction. Both surfaces can read the repo in conversation. The asymmetry I just built an entire architectural argument around doesn't exist — or at least doesn't exist in the direction I claimed.

Let me figure out what this actually does before rebuilding on wrong assumptions again.Well. Claude.ai's GitHub integration is available on all plans including Free. You click the "+" button, select "Add from GitHub," browse repositories, and select specific files and folders. Claude accesses and processes the selected content when you send your message. And critically, for Projects specifically, you can add GitHub content to project knowledge, search through accessible repositories or paste a URL, use the file browser to select specific files and folders, and click "Sync now" to fetch the latest changes.

That means this very Project — Schema-3 — could have the repo connected as project knowledge. The grammar layer, the scaffolds, the specifications, HANDOFF-CURRENT.md — all synced from GitHub, searchable via `project_knowledge_search`, refreshable with one click. The manually-uploaded files in `/mnt/project/` that I've been searching all session could be replaced by a live repo connection.

And ChatGPT's GitHub connector provides the same read access on its side. ChatGPT's GitHub app lets you read from repositories to analyze and search code — it pulls live data and reasons over it in real time.

So the actual symmetry is: **both chat surfaces read from the same repo. Neither writes to it from the chat surface. The CLI agents write to it.** The transcription tax between Claude.ai and ChatGPT dissolves — not because they talk to each other, but because they both read from the same source of truth, and the CLI fleet maintains that source of truth underneath.

This changes the proposal from a 494-line infrastructure document into something much tighter. Let me produce it.169 lines. Down from 494. The compression happened because the insight — both surfaces read from the same repo, the CLI fleet writes to it, the operator commits — makes most of the infrastructure detail unnecessary in the primary document. The v1 remains available as the detailed reference for per-surface configuration specifics (the Claude Code hooks, the AGENTS.md expansion guidance, the Grok tokenomics table, the Obsidian plugin ecosystem) when you actually need to configure each surface.

The thing your screenshot revealed is structurally more important than anything the previous five turns of research uncovered. The "Add from GitHub" button in Claude.ai means the entire framing of the problem changes. It's not "how do we get chat surfaces to share context" — the mechanism already exists and you're sitting in front of it. It's "how do we organize what they share." Which is a directory structure question, a governance question, and a discipline question. All of which the grammar layer already answers.

The closing line of the proposal — "the repo is the constitution, the chat surfaces are the deliberation chambers, the CLI fleet is the civil service, the operator is the sovereign" — isn't decorative. It's the actual structural model. And it maps cleanly onto the grammar layer's authority lattice: canon lives in `grammar/` (constitutional), scaffolds and specifications live in their directories (legislative), production happens via the fleet (executive), and the operator retains merge authority (sovereign). The knowledge architecture's own governance model governs the operational architecture that produces it. Recursive coherence.