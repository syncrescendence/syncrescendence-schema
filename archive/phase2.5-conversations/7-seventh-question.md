Indeed, we want to avoid incurring API costs always  and would rather circumvent one way or another to using the subscription OAuth. If it's just an API Key that will meter from the subscription then that's fine, obviously, or MCP or whatever.
 I only bring up Grok because its relevance depends on whether or not we can access Grok 4.20, via the CLI somehow, only metering from SuperGrok. But we might have leftover API usage from xAI, another perhaps solution for no Grok 4.20.
Same goes for Perplexity, via CLI, metering only from Perplexity Pro. 
Finally you're going to be a direct liaison for Claude Code (desktop) and Cowork, since you have/can mine full context from this thread i.e. writing or eliciting the config. 
I have not transcribe the past 5 or so interactions here. But everything else exists as you see in the tree file.
Conduct any last websearches for clarification. expand the verification holistically.
If we have enough definitive answers on all the aforementioned, begin to clarify this approach into a lucid, incisive, efficacious proposal. Draft it as an artifact. 
We'll refine it until it meets our expectation.