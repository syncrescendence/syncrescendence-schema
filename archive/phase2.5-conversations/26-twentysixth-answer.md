Six sections. Section I photographs the current filesystem exactly as tree.txt shows it — the flat root with 113 turn-prefixed files, the `_/` grammar directory, the phase1 archive, the provenance corpus. Section II defines where everything goes. Section III is the mutation plan — fourteen steps, each a concrete bash operation Commander can execute. Section IV provides the actual file contents for every config file: CLAUDE.md, AGENTS.md, GEMINI.md, settings.json with grammar-directory deny rules, PROTOCOL.md with write discipline and research cycle protocol, and .gitignore. Section V scopes the first research calibration cycle against Slack. Section VI tells Vanguard exactly what to inspect.

The mutation plan uses copy rather than move for grammar files and accepted artifacts (preserving the original folder as fallback until the repo is proven), and move for conversation archives and superseded versions. The version-resolution heuristic — highest turn number is the accepted version — needs Commander to verify by inspection in a few ambiguous cases (SCAFFOLD-SLACK.md exists at both 01- and 02-).

The path from here: you paste this spec to Vanguard with the tree.txt and the Vanguard methodology exemplar attached. Vanguard inspects the five questions in Section VI — directory structure correctness, config file adequacy, mutation plan completeness, research cycle scoping, prematurity check. Vanguard's corrections come back through you. I incorporate them. Then Commander or Cowork executes the mutation against the actual folder on your machine. The repo exists. The first calibration cycle runs. The apparatus starts producing the evidence that everything downstream depends on.