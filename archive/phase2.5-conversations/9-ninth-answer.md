This is a significant expansion of the operational surface. Let me capture what just changed and update the proposal.

**Gemini CLI** is a full CLI participant, subscription-metered. If you're subscribed to Google AI Pro, you automatically get higher limits to Gemini Code Assist and Gemini CLI — shared across CLI and agent mode in the IDE. The free tier alone provides 60 requests/min and 1,000 requests/day with Gemini 2.5 Pro and a 1M token context window. With your Google AI Pro subscription, those limits are substantially higher. Uses `GEMINI.md` for context files (same pattern as CLAUDE.md and AGENTS.md). Supports MCP. Open source. This is a third fully subscription-metered CLI agent.

**Obsidian CLI** shipped with Obsidian 1.12. It gives CLI access to tags, properties, backlinks, and link hierarchy — the same results you'd see in Obsidian's search bar, piped to stdout. Without it, getting backlink data means opening every file and parsing wikilinks in memory. The CLI gets you ~85% of what Obsidian knows (missing only visual features like canvas rendering and graph animation). Multiple plugins now bring Claude Code, Codex, and Gemini CLI directly into the Obsidian vault — your vault becomes a live workspace, not a storage layer, and agent sessions become part of the knowledge graph.

**Claude in Chrome** is the web↔CLI bridge you were looking for. Claude Code integrates with the Chrome extension to give browser automation from the CLI — Claude opens tabs, shares your browser's login state, and can access any site you're already signed into. You can schedule shortcuts to run automatically — daily, weekly, monthly, or annually. This means you can schedule a Chrome shortcut that runs a Perplexity Deep Research session, saves the results, and Claude Code picks them up. That's the Perplexity bridge without manual relay.

**OpenClaw** — you mention it's working. That's a daemon/runtime surface we already scaffolded. In the CLI architecture it operates as the autonomous agent layer — persistent, self-reporting, economically sovereign (if configured). The harness-taxonomy § 6 primitives are live, not theoretical.

**Google Workspace CLI (gws)** — the MCP server that gives direct access to Drive, Gmail, Calendar, Sheets. Combined with your Google AI Pro subscription, this is the Google ecosystem integration layer.

Let me update the proposal to incorporate all of this.The proposal needs substantial additions across multiple sections. Rather than doing twenty surgical edits, let me rebuild the relevant sections coherently. The core structure stays — I'm adding Gemini CLI, Obsidian CLI, Claude in Chrome, OpenClaw, and gws as new operational surfaces, plus updating the operational model, directory structure, and open questions.Now add configuration subsections for the new surfaces in Section 3.