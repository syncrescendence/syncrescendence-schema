Vanguard's inspection is precise and every correction lands. Seven revisions to the spec, none structural, all strengthening.

**Correction 1 — Active dossiers promoted out of provenance.** The fourteen SaaS reports in `provenance/2-exocortex/` are active evidential substrate, not archival. The scaffold grammar is explicit: dossiers remain active after scaffold production. They move to `dossiers/saas/` as active evidence. `provenance/2-exocortex/` retains whatever non-dossier material existed there (if any) or becomes an empty provenance slot.

**Correction 2 — Phase 1 active substrate preserved.** `PHASE-TRANSITION-1-TO-2.md` and pre-build memos are not archivable yet (the five-condition archival test hasn't passed). These get promoted to a visible location rather than buried in `archive/phase1/`. The rest of Phase 1 conversation lineage (Q&A pairs, intermediate drafts) can archive.

**Correction 3 — Supersession as pedigree, not disposal.** `archive/superseded/` becomes `archive/pedigree/`. Files moved there retain their identifiers and get a `Superseded-by:` header linking to the successor. This preserves the naming law's resolvability promise.

**Correction 4 — Two-phase settings regime.** Bootstrap mode allows filesystem operations (mkdir, cp, mv, ln) under Sovereign review. Production mode locks grammar-directory protection and tightens permissions. The switch happens after the initial commit.

**Correction 5 — `.mcp.json` included at bootstrap.** Even if MCP servers aren't configured immediately, the file exists as a placeholder declaring the intended bridges.

**Correction 6 — Version resolution tightened.** "Highest turn number is candidate successor, subject to acceptance evidence" replaces the naive chronological heuristic. Commander diffs ambiguous cases and annotates rather than crowning by numerology.

**Correction 7 — Research cycle reframed.** The Slack calibration cycle defaults to "measure adequacy, expose blind spots, decide whether revision is warranted" — not to producing SCAFFOLD-SLACK-v2.md. Output options are: no-action confirmation, remediation note, or conditional draft successor with explicit supersession intent.

**Plus the Bootstrap A/B split.** Bootstrap A creates the repo, directories, minimum instructions, and one protected handoff path. Bootstrap B (after the first receipt-bearing calibration cycle) installs stricter protocol, wider agent configs, and automation surfaces.Every Vanguard correction is incorporated. The spec is now structurally honest: Bootstrap A creates the repo with permissive settings and minimum governance. The first calibration cycle runs against Slack. Bootstrap B locks the repo and installs production governance from observed patterns. The dossiers are active evidence. The pedigree trail is preserved. The settings regime admits that the bootstrap itself needs filesystem permissions the production mode denies.

The path from here: you take the spec to your machine. Commander (Claude Code) or Cowork executes Bootstrap A against the actual folder. You review the commit diff. Push. Connect both chat surfaces. Then we run the Slack calibration cycle — the first grammar-aligned research the institution has ever conducted.