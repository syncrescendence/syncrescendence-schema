The passages you're highlighting converge on a single structural diagnosis that should be the first line of any CLI research commission. Let me extract the load-bearing sentences:

**The inversion that never happened:**
> "The grammar layer was supposed to be the *lens* through which research gets conducted, not the machine that formats pre-existing research into nicer containers."

**What actually happened:**
> "Primitives that the dossier authors didn't think to investigate never got investigated. Axes where the dossier evidence was thin got thin scaffold entries. The scaffold looked complete because I populated every axis, but the population was constrained by what the unaligned research happened to cover, not by what the axes actually demand."

**The context-injection problem — the actual technical challenge:**
> "The question is whether the grammar layer itself can substitute for that lost context by providing the research agent with axis-specific decomposition targets, epistemic-labeling requirements, and convergence criteria. That's the context-injection design problem."

**What grammar-aligned research would actually look like:**
> "Decompose this surface along these nine axes. For each axis, identify the primitive, label its epistemic status, cite your source, and flag where your evidence is thin."

**The convergence question that was never asked:**
> "Given these 14 sampled surfaces, what does the primitive distribution tell us about the *class*? Do we need 30 surfaces? 50? Is there a convergence threshold where adding more surfaces stops producing new convergent primitives?"

This is what the CLI fleet is for. Not infrastructure. Not governance. Not the cathedral. The fleet exists to solve the context-injection problem at scale — injecting the grammar layer's axis sets, epistemic requirements, and convergence criteria directly into research agents' prompts, something the pre-grammar Grok sessions couldn't do because the grammar didn't exist yet, and something the chat-relay sessions couldn't do efficiently because the transcription tax made each research cycle expensive.

The CLI advantage is concrete and specific: Grok 4.1 Fast's 2M context window can hold the entire grammar layer plus the target modality class's axis set plus the scaffold grammar's twelve-check schema plus the specific research question — in a single prompt. That's the context injection that was missing. The grammar layer IS the prompt. You don't need the lost conversational context from the original Grok sessions because the grammar layer encodes what that context was groping toward.

Codex can run that prompt in parallel across multiple surfaces. Gemini CLI can run it with a different cognitive signature, producing divergent decompositions. Adjudicator can audit the results against the twelve-check schema. The triangulation isn't three models producing the same generic exegesis. It's three models decomposing along the same grammar-defined axes, producing evidence that's directly scaffold-ready because the axes were in the prompt from the start.

The first CLI research cycle should target a surface that already has a pre-grammar scaffold — say, Slack. Run the grammar-aligned prompt through the CLI fleet. Compare the output to the existing SCAFFOLD-SLACK.md. The delta between them reveals exactly what the pre-grammar research missed: primitives that the original dossier didn't investigate because no axis demanded them, epistemic gaps that the original exegesis papered over with narrative, convergence questions that the original 14-surface sample couldn't answer because nobody asked.

That delta is the empirical proof of the research methodology's value. And it generates the first real operational receipts that Phase 3's Lane B contracts need. One cycle, two problems solved.

Want me to produce that first grammar-aligned research commission — the one that targets a known surface to calibrate the methodology before running it on unknowns?