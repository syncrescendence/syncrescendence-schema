No, you're right — that's an overstatement on my part. The CLI agents need the grammar files somewhere they can read them, and the research outputs need somewhere to land. The agents can't inject the grammar layer as context if the grammar layer lives only in a Claude.ai project's uploaded files.

The distinction is between the lightweight working directory and the constitutional repo. You already said the officially sanctioned repo gets built from scratch once we know everything. What we need right now is a folder with the nine grammar documents, the axis sets, and the scaffold grammar's twelve-check schema — enough that a CLI agent can be pointed at it and told "load these as your decomposition frame." A `git init` and a push to GitHub gives both chat surfaces read access via their connectors, which is useful but not required for the first cycle. The CLI agents just need filesystem access.

So: create the minimal working directory, populate it with the grammar layer, run the first research cycle, let the cycle's outputs and friction reveal what the directory structure actually needs to be. The constitutional repo emerges from that evidence. The working directory is scaffolding, not canon.